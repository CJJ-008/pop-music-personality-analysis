# 技术栈通俗说明

> 这份文档解释本项目用到的每一个工具**具体做了什么**。每个工具都配了生活中的比喻，
> 并且说明它在本项目里真实干的活（带项目里的实际数字）。
>
> 项目一句话概括：**用 1010 个年轻人的性格问卷 + 28356 首 Spotify 歌曲数据，
> 找出"什么性格的人喜欢什么歌手"。**

---

## 零、先看一个总比喻

把整个项目想象成开了一家 **"音乐口味侦探所"**：

- **数据源（Kaggle、TidyTuesday）** = 两个图书馆，一个存着"人的性格档案"，一个存着"歌曲档案"
- **kagglehub、urllib** = 跑腿的取件员，负责把档案从图书馆搬回来
- **pandas、NumPy** = 档案整理台，把乱七八糟的原始档案整理成整齐的表格
- **scikit-learn** = 侦探团队，负责从档案里找出规律（这是项目的核心）
- **SciPy** = 质检员，判断侦探找到的规律是真线索还是巧合
- **Matplotlib、Seaborn** = 画师，把结论画成图给人看
- **Jupyter** = 办案记录本
- **Streamlit、Plotly** = 自助查询大厅，客户自己点播："我想看外向型的人喜欢谁"——点一下，图和名单立刻变
- **streamlit-authenticator、bcrypt、验证码** = 大厅门口的门卫、登记处和"人机小测验"，凭证核对通过才能进
- **AppTest** = 出厂检测线，每次上线前把整个应用真实跑一遍逐项检查
- **Git、GitHub** = 存档系统，防止办案资料丢失

下面逐个说清楚。

---

## 一、数据从哪来

### 1. Kaggle —— 数据集"超市"

**它是什么**：全球最大的公开数据集网站，任何人都能上传和下载数据。

**比喻**：像一个巨大的**免费超市**，货架上摆着别人整理好的数据。你要做分析，先去超市挑食材，不用自己从种地开始。

**在本项目里干了什么**：
提供了"性格档案"——**Young People Survey** 数据集，1010 名 15~30 岁年轻人的问卷，
共 150 列，包含 17 个音乐流派的打分（1~5 分）和一堆性格、生活习惯题目。

> 网址：https://www.kaggle.com/datasets/miroslavsabo/young-people-survey

---

### 2. TidyTuesday（GitHub 上的数据项目）—— 另一个数据集来源

**它是什么**：一个每周发布一份有趣数据集的公益项目，数据放在 GitHub 上。

**比喻**：像**每周免费发一份食材包**的美食节目，这周发海鲜，下周发蔬菜，谁都能拿去练手。

**在本项目里干了什么**：
提供了"歌曲档案"——**Spotify Songs** 数据集，32833 行歌曲记录，
含歌手名、流派、流行度（0~100）、11 个音频特征（舞蹈性、能量、情绪效价等）。
它有个大好处：**放在 GitHub 上，不用注册账号就能直接下载**。

> 网址：https://github.com/rfordatascience/tidytuesday/tree/main/data/2020/2020-01-21

---

### 3. kagglehub —— Kaggle 的"自动取件员"

**它是什么**：Kaggle 官方出的 Python 小工具，用一行代码就能把数据集下载到本地。

**比喻**：像**外卖 App**。你不用自己去超市排队结账，点一下，东西送到家。

**在本项目里干了什么**：
本来我以为下载 Kaggle 数据必须注册账号手动点下载按钮，都准备好让你去注册了。
结果试着用 kagglehub 匿名下载，**竟然直接成功了**——于是你不用注册 Kaggle 了。

```python
import kagglehub
path = kagglehub.dataset_download("miroslavsabo/young-people-survey")
```

---

### 4. urllib —— Python 自带的"搬运工"

**它是什么**：Python 标准库里的网络下载模块，不用额外安装。

**比喻**：像**自己开车去取货**。外卖 App 不好使的时候，自己跑一趟也能拿到。

**在本项目里干了什么**：
从 GitHub 下载那个 7MB 的 Spotify 歌曲 CSV 文件（`spotify_songs.csv`）。
代码在 `src/01_download_data.py`，还带了校验：下载完检查是不是真的有 23 列，防止下到半截的坏文件。

---

## 二、把数据洗干净（数据处理）

### 5. pandas —— "Excel 超级加强版"

**它是什么**：Python 里处理表格数据的核心工具，提供 `DataFrame`（数据框）这个结构。

**比喻**：**Excel 的超级赛亚人形态**。Excel 能干的它都能干，但：
- Excel 点鼠标筛选，它写一行代码
- Excel 处理几万行会卡，它处理几百万行很轻松
- Excel 的操作没法记录，它的每一步都写在代码里，别人能原样复现

**在本项目里干了什么**（这是用得最多的工具）：

| 具体活儿 | 例子 |
|---|---|
| 读数据 | 把两个 CSV 读进内存 |
| 看数据长什么样 | 发现问卷 150 列里有 144 列存在缺失，但总缺失率只有 0.4% |
| 填缺失值 | 打分题用"中位数"填，文字选项题用"出现最多的那个"填 |
| 合成新列 | 把 7 道相关题目平均成 1 个"外向社交"分数（详见下面第六节的性格维度） |
| 分组统计 | 算出"每个性格画像的平均年龄、女性占比" |
| 合并表格 | 把问卷的流派偏好表和歌曲的歌手表拼起来 |
| 去重 | 发现 32833 行里有 4477 行是同一首歌重复出现，去重后剩 28356 首 |
| 存结果 | 22 张结果表存成 CSV，用 `utf-8-sig` 编码让 Excel 打开不乱码 |

**一个真实的坑（面试可以讲）**：
pandas 3.0 版本改了字符串类型的名字（从 `object` 变成 `str`）。
我原本把"守时"这列当成 1~5 分的打分题，结果它其实是文字选项
（"我总是很准时" / "我经常迟到"）。
程序直接报错 `Cannot perform reduction 'median' with string dtype`——**这反而是好事**，
报错总比悄悄算错强。后来我把这类文字题按顺序编码成 1~5 分，并且加了一道检查，
以后再遇到非数值的列会直接警告。

---

### 6. NumPy —— "计算引擎"

**它是什么**：Python 做数值计算的地基库，pandas 底层也是靠它。

**比喻**：像汽车的**发动机**。你开车时感觉不到它，但没有它车根本动不了。
pandas 是方向盘和仪表盘，NumPy 是发动机。

**在本项目里干了什么**：
- 提供高效的数组运算（几千个数字一起加减乘除）
- 算平方根、均值等数学运算（比如把均方误差开根号得到 RMSE）
- 固定随机种子 `RANDOM_STATE = 42`，保证每次跑出来的结果一模一样

> **为什么固定随机种子很重要**：K-Means 聚类开始时是"随机"选起点的。
> 不固定种子的话，你今天跑出 3 个画像，明天跑出 4 个，没法写进报告。
> 固定成 42（这是程序员的梗，来自《银河系漫游指南》里的"生命宇宙及一切的答案"），
> 每次结果就完全一致——我实测过，把项目复制到另一个文件夹重跑，结果表逐字节相同。

---

## 三、侦探团队：机器学习算法（scikit-learn）

**scikit-learn 是什么**：Python 最常用的机器学习库，把各种算法打包成统一接口。

**比喻**：像一个**侦探事务所**，里面雇了各种专长的侦探：
有擅长分组的、有擅长分类的、有擅长预测数字的。
你不用自己从零培养侦探，直接"雇"一个来用就行。

本项目一共用了 8 种算法/工具，分三类：**聚类（分组）、分类（贴标签）、回归（预测数值）**。

---

### 第一类：聚类 —— "把相似的人自动分成几堆"

#### 7. K-Means（K 均值聚类）—— 主力分组算法

**它是什么**：最经典的聚类算法，把数据自动分成 K 组。

**比喻**：**聚会时自动分堆**。
假设一屋子陌生人要按兴趣分成 3 堆：
1. 先随便点 3 个人当"堆长"（随机选起点）
2. 每个人走向离自己兴趣最近的那个堆长
3. 堆长挪到这堆人的正中间
4. 重复 2~3 步，直到大家不再换堆

**在本项目里干了什么**：
把 1010 个年轻人按 5 个性格维度分成了 **3 个性格画像**：

| 画像 | 人数 | 占比 |
|---|---|---|
| 开放好奇·宜人友善型 | 359 | 35.5% |
| 情绪稳定·外向社交型 | 343 | 34.0% |
| 尽责自律·宜人友善型 | 308 | 30.5% |

代码在 `src/03_cluster_personality.py`。

---

#### 8. 肘部法则 + 轮廓系数 —— "到底该分成几堆？"

**问题**：K-Means 要你先告诉它分成几组（K=几）。但你怎么知道是 3 组还是 5 组？

**比喻**：
- **肘部法则**像**看收益曲线**。你雇第 1 个员工带来 100 的收益，第 2 个带来 60，
  第 3 个带来 30，第 4 个只带来 5——那第 4 个人就不太值得雇了。
  画成图，曲线会像一个手肘弯下去，弯点就是合适的组数。
- **轮廓系数**像**给分组质量打分**。它同时看两件事：
  "我这堆人抱团紧不紧" + "我这堆离别的堆远不远"。
  分数范围 -1 到 1，越接近 1 说明分得越干净漂亮。

**在本项目里干了什么**：
把 K=2 到 K=10 全试了一遍，算出每个 K 的轮廓系数。
结果发现**所有 K 的轮廓系数都低于 0.2**（最高才 0.187）。

**这个发现很重要，我如实写进了报告**：它说明年轻人的性格**不存在泾渭分明的分群**，
人是连续的，不是"要么外向要么内向"这么简单。

那最后怎么定的 K=3？我用的规则是：
**"取轮廓系数 ≥ 最优值 90% 里最大的那个 K"**。
K=2 分数最高（0.187）但只分 2 类太粗糙、没业务价值；
K=3（0.170）质量损失很小，但画像更有用。

> 这个规则写在代码注释和 README 里，面试被问到能讲清楚逻辑，不是随便拍的。

---

#### 9. 层次聚类（Ward 连接）—— "另一个侦探，用来交叉验证"

**它是什么**：另一种聚类算法，思路完全不同。

**比喻**：**画家谱**。
先把最像的两个人合成一家，再把最像的两家合成一族，一层层合并上去，
最后形成一棵大树。你想要几组，就在树上横着切一刀。

**在本项目里干了什么**：
用它独立分了一次组，然后跟 K-Means 的结果对比。
用 **ARI（调整兰德指数）** 衡量两种算法结论有多像，结果 ARI = 0.364。

**ARI 的比喻**：**两位老师给同一批学生分组，结果有多像？**
1 = 完全一样，0 = 跟瞎分差不多。0.364 表示"有一定共识但不完全一致"——
这也印证了前面"人群没有清晰分界"的结论。

---

#### 10. PCA（主成分分析）—— "把 3D 压成 2D 好画图"

**它是什么**：降维算法，把很多个特征压缩成少数几个。

**比喻**：**给一个立体的雕像拍照**。
雕像有三维，照片只有二维，但好的摄影师会挑角度，
让照片尽量保留雕像的信息（能看出是人还是狗、什么姿势）。
PCA 就是自动找那个"最好的拍摄角度"。

**在本项目里干了什么**：
5 个性格维度没法直接画在平面上（人的眼睛只能看 2 维）。
PCA 把它压成 2 个维度，画出了图 9 那张聚类散点图。
两个主成分分别保留了 31.3% 和 22.8% 的信息，合计约 54%。

**这张图恰好证明了前面的结论**：三堆颜色混在一起、边界模糊——
一眼就能看出"人群性格没有清晰分界"。

---

### 第二类：分类 —— "给新人贴标签"

**要解决的问题**：来了一个新人，只知道他的年龄、性别、兴趣爱好，
**能不能猜出他属于哪个性格画像**？（猜出来就能推荐音乐了）

#### 11. 逻辑回归 —— "画一条分界线"

**它是什么**：虽然名字里有"回归"，其实是分类算法。

**比喻**：**用一根棍子把桌上的红豆绿豆分开**。
它找一条最合适的分界线，线这边归 A 类，那边归 B 类。
（多分类时就是找好几条线划分区域）

**优点**：简单、快、结果好解释（能看出每个特征往哪个方向推）。

**在本项目里干了什么**：准确率 **67.6%**，表现相当不错。
代码里前面接了个 StandardScaler（见下面第 15 条），因为逻辑回归对数据尺度敏感。

---

#### 12. 决策树 —— "一连串是非题"

**它是什么**：通过一系列"是/否"判断来做分类。

**比喻**：**医院分诊台**。
"发烧吗？"→ 是 → "咳嗽吗？"→ 是 → 判断为呼吸道感染。
决策树就是自动学出这一串问题，每个问题都尽量把人群分得最开。

**优点**：规则一目了然，能直接读出来。
**缺点**：容易"死记硬背"（过拟合），所以我在代码里限制了树的深度（`max_depth=6`）。

**在本项目里干了什么**：准确率 **57.3%**，三个模型里最差。
这也合理——性格和兴趣的关系不是简单的"是/否"能概括的。

---

#### 13. 随机森林 —— 本项目表现最好的分类器

**它是什么**：种很多棵决策树，让它们投票。

**比喻**：**叫 400 个专家各看一遍，少数服从多数**。
一个专家可能看走眼，但 400 个专家投票，准确率就上来了。
而且每个专家只看一部分资料、只回答一部分问题（这叫"随机"），
这样他们的错误不会雷同，互相能纠偏。

**在本项目里干了什么**：
准确率 **69.2%**，三个模型里最好。对比"全猜最多类"的基线 35.5%，
**提升了 33.7 个百分点**——几乎翻倍，说明真的学到了东西。

代码里设了 `n_estimators=400`，就是"雇 400 个专家"。

---

#### 14. 交叉验证 —— "模拟考试，防止背答案"

**它是什么**：评估模型真实水平的标准做法。

**比喻**：**把题库分成 5 份，轮流拿 4 份当练习、1 份当考卷**，考 5 次取平均。
如果只拿一套题练、同一套题考，模型可能只是"背下了答案"（过拟合），
换个新题就露馅。

**在本项目里干了什么**：
所有分类和回归结果都是 5 折交叉验证得出的，不是"自己考自己"的虚高分。

> **顺便说一个我特意避开的陷阱（面试加分点）**：
> 原始歌曲数据里，同一首歌会因为出现在多个歌单而重复出现（4477 行）。
> 如果不去重，同一首歌可能既在训练集里、又在测试集里——
> 相当于**考试前把答案偷偷塞给学生**，分数会虚高。
> 我在建模前按 `track_id` 去重了，这是保证结果可信的关键一步。

---

#### 15. StandardScaler —— "统一度量衡"

**它是什么**：数据标准化，把所有特征缩放到同一尺度。

**比喻**：**秦始皇统一度量衡**。
如果比身高（170 厘米）和比体重（60 公斤）直接放一起算，
数字大的那个（170）会显得更重要，这不公平。
标准化就是把两者都换算成"在人群中排第几"的相对位置，这样才公平可比。

**在本项目里干了什么**：
聚类前把所有性格维度标准化（否则某个维度数值天然大就会主导分组结果）；
逻辑回归前也做了标准化。

---

### 第三类：回归 —— "预测一个具体数字"

**要解决的问题**：一首歌的**流行度**（0~100 分）能预测吗？什么样的歌容易火？

#### 16. 线性回归 —— "画一条最贴合的趋势线"

**它是什么**：最基础的回归算法，找一条直线去拟合数据。

**比喻**：**在散点图上画一条最"公道"的直线**，
让所有点到这条线的距离总和最小。

**在本项目里干了什么**：作为**基线**（benchmark），R² = 0.087。
意思是"只能用线性关系解释 8.7% 的流行度差异"，效果很一般。

> **为什么要放个效果一般的基线？** 因为要证明"复杂模型确实比简单模型强"。
> 如果随机森林只比线性回归好一点点，那就不值得用复杂模型。

---

#### 17. 随机森林回归 —— "还是那 400 个专家，这次预测数字"

**它是什么**：同一个随机森林，改成预测连续数值而不是分类。

**在本项目里干了什么**：R² = **0.173**，比线性回归的 0.087 好一倍。

**还做了个"消融实验"**（面试可以讲的名词）：对比两种输入
- 只用音频特征 + 年份：R² = 0.135
- 再加上流派信息：R² = **0.173**

这说明**流派是有效信号**，加进去确实有帮助。这叫消融实验——
"把某个部分拿掉，看效果掉多少，就知道它有多重要"。

---

#### 18. 评估指标（R²、RMSE、MAE、准确率、F1）—— "考卷的评分标准"

**比喻**：不同的考试科目要用不同的评分标准。

| 指标 | 比喻 | 本项目结果 |
|---|---|---|
| **准确率** | 100 道题对了几道 | 分类 69.2% |
| **宏平均 F1** | 每类分别打分再平均，防止"大类别刷分" | 0.686 |
| **R²** | "能解释多少比例的变化"，1 是满分 | 回归 0.173 |
| **RMSE** | 平均预测误差（单位跟原数据一样，好理解） | 21.56 分 |
| **MAE** | 平均绝对误差，比 RMSE 更不受极端值影响 | 17.76 分 |

> **为什么分类要看 F1 而不只看准确率？**
> 假设 100 人里 90 个是 A 类，模型全猜 A 就有 90% 准确率，但它其实啥也没学会。
> F1 会把这种"刷分"行为暴露出来。

---

#### 19. 特征重要性 —— "找出最关键的线索"

**它是什么**：随机森林自带的功能，告诉你哪个特征对预测贡献最大。

**比喻**：**侦探复盘时排线索重要性**。
"这次破案，目击者证词贡献 40%，监控录像 30%，现场指纹 20%……"

**在本项目里干了什么**（这是很有意思的发现）：

预测性格画像，最重要的线索是：
1. 极限运动（0.0545）
2. 艺术展览（0.0524）
3. 戏剧（0.0427）
4. 跳舞（0.0409）
5. 身高（0.0317）

→ 说明**"生活方式就是音乐口味的镜子"**：爱看展看戏的人 vs 爱玩极限运动的人，
确实是两类人。

预测歌曲流行度，最重要的因素是：
1. **发行年份**（0.1294）—— 远超其他所有因素
2. loudness 响度（0.0860）
3. duration_ms 时长（0.0785）

→ **发行年份排第一**，说明流行度这个指标有"新歌优势"：
Spotify 的流行度算的是**当前**热度，新歌天然占便宜。
这是个值得在报告里说明的指标特性，不是"老歌不好听"。

---

## 四、质检员：统计检验（SciPy）

### 20. SciPy —— "科学计算的工具箱"

**它是什么**：Python 的科学计算库，本项目主要用它的**统计检验**功能。

**比喻**：像**质检员**。侦探（机器学习）说"我发现了规律"，
质检员负责验证"这规律是真的，还是运气好碰上的"。

---

### 21. Kruskal-Wallis 检验 —— "判断差异是真本事还是运气"

**它是什么**：一种非参数统计检验，用来判断多组数据之间是否有显著差异。

**比喻**：**判断三个班的平均分差异是"教学水平不同"还是"随机波动"**。
两个班平均分差 2 分，可能只是运气；差 20 分，那就真有差别了。
这个检验会算出一个 p 值，告诉你"这种差异纯属巧合的概率有多大"。

**为什么要用它，而不是普通的 t 检验/方差分析？**
因为问卷打分是 1~5 分的**等级数据**，不满足"正态分布"这个前提假设。
Kruskal-Wallis 用的是**排名**而不是平均值，对数据分布没有苛刻要求，
所以更适合这种问卷数据。

**在本项目里干了什么**（这是让结论站得住脚的关键一步）：

我把 3 个性格画像在 17 个流派上的偏好做了检验，结果：
**15 个流派差异显著（p < 0.05），只有 2 个不显著**。

差异最大的前几名：

| 流派 | H 统计量 | p 值 | 是否显著 |
|---|---|---|---|
| 音乐剧 | 82.19 | 0.0000 | 显著 |
| 歌剧 | 77.61 | 0.0000 | 显著 |
| 拉丁 | 65.16 | 0.0000 | 显著 |
| 爵士 | 61.84 | 0.0000 | 显著 |
| 古典 | 61.78 | 0.0000 | 显著 |
| … | | | |
| 朋克 | 2.84 | 0.2415 | **不显著** |
| 金属/硬摇 | 1.73 | 0.4221 | **不显著** |

**不显著的那两个我直接标注出来，没有当成结论。**
如果不做这步检验，看到"朋克在某个画像里分数高 0.05"就下结论，那就是把噪声当发现了。

---

## 五、画师：可视化（Matplotlib、Seaborn）

### 22. Matplotlib —— "画笔和画布"

**它是什么**：Python 最基础的画图库，什么图都能画。

**比喻**：**一套基础画笔和画布**。你想画什么都能画，但每一笔都得自己调，
画出来默认样子比较朴素（有点像上世纪统计软件的风格）。

**在本项目里干了什么**：
18 张图里所有图都是它画出来的——柱状图、直方图、散点图、箱线图、热力图。

**还解决了一个中文显示的坑**：
Matplotlib 默认字体不含中文，直接画中文会显示成一堆方框（俗称"豆腐块"）。
我在 `src/config.py` 里统一设置了中文字体 `Microsoft YaHei`，并解决负号显示问题。

**另一个真实的坑（面试可以讲）**：
有个歌手的名字真的叫 **`$uicideBoy$`**（数据里就是用 `$` 代替 `S` 的风格化写法）。
结果 matplotlib 把 `$...$` 当成 **LaTeX 数学公式**解析了，
两个美元符号被吞掉，文字还变成了斜体，图上显示成"uicideBoy"。
我一开始以为是标签被裁掉了，查了数据才发现是渲染问题，
最后通过转义 `$` 符号解决。**这个坑很隐蔽，值得记住。**

---

### 23. Seaborn —— "精装画笔套装"

**它是什么**：基于 Matplotlib 的高级封装，默认样式更好看。

**比喻**：**Matplotlib 是基础画笔，Seaborn 是精装套装**——
自带好看的配色方案和现成模板，画统计图（热力图、箱线图）特别省事。

**在本项目里干了什么**：
- 相关性热力图（图 3、图 6、图 10、图 11、图 12、图 17）
- 各流派流行度箱线图（图 7）
- 混淆矩阵热力图（图 13）

配色用了 `RdBu_r`（红蓝对比色，适合表现正负差异）和 `YlOrRd`（黄橙红渐深，适合表现强度）。

---

## 六、出报告（展示与记录）

### 24. Jupyter Notebook —— "实验记录本"

**它是什么**：能在浏览器里写代码、跑代码、看结果、写笔记的工具。

**比喻**：**科学家的实验记录本**。
左边写"我做了什么、为什么这么做"，右边贴实验结果的图和数据，
而且代码可以现场重跑——别人翻开就能完整复现你的实验。

**在本项目里干了什么**：
`reports/流行音乐数据分析报告.ipynb`，26 个单元格，
完整记录了从数据介绍到结论局限的全过程，**已实际执行验证 0 报错**。
面试时直接打开这个文件展示，比讲 PPT 有说服力。

---

### 25. nbformat —— "自动排版工具"

**它是什么**：用代码生成 Jupyter Notebook 文件的库。

**比喻**：**用模板批量生成报告**，而不是手打一份份文档。

**在本项目里干了什么**：
我写了 `src/07_build_report.py` 这个脚本，用代码把报告生成出来。
**为什么这么做？** 因为如果数据更新了，重跑脚本就能生成新报告，
不用手动改 26 个单元格。这也是一种工程化习惯。

它的搭档 **nbconvert** 负责报告的"二次分发"：把 Notebook 一键导出成
**自包含 HTML 单文件**（图表全部内嵌，约 2MB），这个人不装 Python 也能
双击打开。导出的 HTML 会打包进 exe 并挂在仪表盘侧边栏——
用户点"下载离线报告"就能把完整分析过程带回家。

---

### 26. CSV + utf-8-sig 编码 —— "人人能打开的表格"

**它是什么**：最通用的表格文件格式；`utf-8-sig` 是一种带 BOM 标记的编码。

**比喻**：**存成大家都认识的语言**。

**在本项目里干了什么**：
22 张结果表都存成 CSV。这里有个中国程序员都会遇到的坑：
Windows 版 Excel 打开普通 UTF-8 的 CSV 时，**中文会变成乱码**。
加上 `sig`（BOM 标记）后，Excel 就知道"这是 UTF-8"，中文正常显示。
这样你双击 CSV 就能用 Excel 打开看，不用写代码。

---

## 六之二、交互式网页仪表盘（项目后期新增）

前面所有工具产出的都是"静态成果"：图是 PNG、报告是 Notebook、表是 CSV。
这一节是项目后期应学生要求新增的**交互式界面**——把分析结果做成一个能自己点播的网页。

### 27. Streamlit —— "自助点播的网页界面"

**它是什么**：把 Python 脚本变成网页应用的框架。写起来像写普通脚本，跑起来是个网站。

**比喻**：Matplotlib 画的图是**挂在墙上的画**——画好就固定了，想看别的得重画。
Streamlit 相当于**把画廊升级成触摸屏查询机**：访客在屏幕上点"我要看外向型的"，
机器立刻把对应的图和名单切换出来，全程不需要程序员在场。

它的写法也省事：就是普通 Python 脚本一行行往下写。每次用户改一个选择，
整个脚本自动从头重跑一遍，界面就刷新了（所以它提供缓存机制，算过的结果不重复算）。

**在本项目里干了什么**：
根目录的 `app.py` 就是它写的——「性格 × 流行歌手推荐器」网页：
- 左侧单选 3 个性格画像之一；
- 右侧立刻显示：该画像的人数/女性占比/年龄/城市占比卡片、
  五维性格雷达图（可以鼠标悬停看数值）、偏爱的流派与相对偏好分、
  该流派 Top 歌手的互动条形图和推荐名单。

一个关键设计：**它只读取 `outputs/tables/` 里已入库的 22 张结果表，完全不碰原始数据**。
所以别人克隆仓库、或部署到云端后，不用下载 7MB 原始数据、不用重跑聚类分类，
打开就能用——这也是它能免费部署上云的前提。

**它自带测试框架（AppTest）**：不开浏览器就能把整个应用真实执行一遍、检查报错。
我上线前跑了三个画像的冒烟测试，还真抓出一个 bug：
歌手排名表把流行度**最低**的排成了第 1 名（排序方向写反了），靠测试才发现修掉。

**又一个 `$` 符号的坑（和前面 matplotlib 的坑同源）**：
网页里 `$uicideBoy$` 同样会被当成 LaTeX 数学公式解析，
所以页面上统一用全角 `＄` 显示（外观几乎一样，但不会被吞掉符号）。

**本地运行**：

```bash
streamlit run app.py    # 浏览器自动打开 http://localhost:8501
```

### 28. Plotly —— "会动的图表"

**它是什么**：交互式图表库，Matplotlib 的"动起来版本"。

**比喻**：**Matplotlib 画的是报纸上的图（静态），Plotly 画的是手机里的图（能动）**——
鼠标悬停显示数值、框选放大、点图例隐藏某条线。

**在本项目里干了什么**：
仪表盘里的两种图都是它画的：
- **性格雷达图**：五个性格维度的 z-score 铺在五边形上，一眼看出这个画像"长什么样"，
  图里还叠加了一条灰色虚线圆环代表"全体平均水平"，高于还是低于平均一眼可辨；
- **歌手流行度条形图**：悬停能额外看到该歌手的歌曲数。

值得记住的分工：
- Matplotlib/Seaborn → 输出 **PNG 静态图**，适合放进报告、论文、简历附件（本项目的 18 张图）
- Plotly → 输出**网页互动图**，适合放进仪表盘让人自己探索

---

### 29. 标签 × 流派亲和度推荐 —— "按性格拼图点菜"

**它是什么**：本项目**自研**的推荐算法（没有现成库可用），把用户勾选的性格标签
翻译成歌手推荐，是仪表盘里"按性格标签找歌手"页面的引擎。

**比喻**：画像页像食堂的**三种固定套餐**，标签页则是**自助餐自选台**——
你勾的每一个标签，后厨都会找出一群真实存在的人（问卷里最符合的前 30%），
把他们实际爱点的菜端给你，而不是从三种套餐里挑一个最接近的应付你。

**四步链路**：
```
勾标签 → 圈参考人群 → 算流派亲和度 → 匹配歌手
```
1. 37 个标签每个都锚定真实问卷列（如「外向爱社交」= 与朋友玩乐+社交+朋友数量
   三道题；双极成对标签用反向计分）；
2. 每个标签给 1010 人打分，取最符合的前 30%（约 300 人）作为参考人群；
3. 流派亲和度 = 参考人群的流派均分 − 全体均分（沿用全项目的"相对偏好"口径），
   预计算成 37×17 矩阵存进结果表，页面秒出；
4. 多标签 = 亲和度向量取平均（外向和内向同时勾选会数学上抵消，页面有提示）；
   歌手综合得分 = 流派亲和度（归一化）× 歌手平均流行度（归一化）。

**在本项目里干了什么**：
- 界面用 Streamlit 的 pills 组件做成 4 组标签墙（性格/生活方式/兴趣/背景）；
- 17 个流派里 5 个（民谣/乡村/古典/音乐剧/歌剧）在歌曲数据集里没有歌手，
  图表里红色标注、不参与匹配——诚实披露数据边界；
- 页面底部可展开"推荐依据"：每个所选标签最偏好的流派、差值、参考人数，全部可核查；
- Sanity 检查确认了已知规律成立（好奇开放→爵士/古典/音乐剧均为正、
  运动健将→说唱 +0.41、小镇青年→民谣/乡村）。

**为什么值得写进简历**：它把推荐从"3 个固定画像查表"升级成"可组合的特征匹配"，
且每一步都能回答"这个推荐怎么来的"——这是推荐系统面试最看重的能力。

**顺带解决的一个交互问题**：37 个标签排成一行会超出屏幕宽度，右侧被静默截断
（容器是 overflow:auto 但没有可见滚动条，鼠标用户根本滚不了）。
解决方式分两半：CSS 用 st.html 注入（样式能生效），JS 用 components.html 的
srcdoc 同源 iframe 去操作父页面 DOM——因为 st.html 里插入的 script 标签
**不会执行**（innerHTML 的规范限制），这是 Streamlit 定制交互的必知坑。

---

### 30. KNN 最近邻测评推荐 —— "找一群和你最像的人"

**它是什么**：本项目自研的第二套推荐算法（仪表盘"性格小测评"页的引擎）。
用户答 25 道题，系统在 1010 名受访者里找出性格最接近的那批人，
用他们的真实音乐偏好做推荐。

**比喻**：**相亲节目里的"性格匹配"环节**。
你填完资料，节目组不给你套模板，而是从资料库里挑出和你最像的 100 个人，
看这群人平时都听什么歌——"和你像的人都在听这些"，比"你属于第 3 类人"更有说服力。

**为什么叫"最近邻"**：把每个人的性格表示成空间里的一个点（本项目是 5 维：
外向社交/尽责自律/情绪稳定/开放好奇/宜人友善），
两点之间的距离越近 = 性格越像。找出离你最近的 k 个点，
k 取总人数的 10%（1010 人 → 101 人）——这就是 KNN（k-Nearest Neighbors）。

**四步链路**：
```
答25题 → 算五维得分 → z标准化后算欧氏距离 → 取最近10%人群 → 用他们的偏好推荐
```
1. 25 道题每维度 5 题，题干锚定问卷原题；「情绪稳定」维度按困扰方向提问，
   计分时反转（6 − 分值），保证高分始终代表"更稳定"；
2. 用户得分要先 **z 标准化**（减人群均值、除标准差）再算距离——
   否则五个维度里数值天然偏大的那个会主导距离计算（和聚类前标准化的道理一样）；
3. 距离用**欧氏距离**（五维空间中两点直线距离），取最近的 101 人；
4. 这 101 人的流派均分 − 全体均分 = 你的流派亲和度，再走和标签页相同的
   流派→歌手映射。

**在本项目里干了什么**：
- `src/09_quiz_recommender.py` 预计算题库、人群均值/标准差、KNN 基础表（入 git），
  页面运行时只做一次距离计算，无需原始问卷数据；
- 结果页展示：你的五维雷达图（含"全体平均"虚线基准）、逐维度与全体对比、
  和你最像的人群的流派偏好条形图、综合推荐 Top10；
- 答不满 25 题会提示"还有题目没答完"，不给出半成品结论。

**为什么值得写进简历**：这是**基于内容的个性化推荐**（Content-based）的完整实现，
和标签页的"特征组合"、画像页的"群体聚类"形成三种互补的推荐范式；
且每一步（题→分→距离→人群→流派→歌手）都能向面试官完整讲清楚。

---

### 31. requirements.txt 与 .python-version —— "配方的精确刻度"

**它是什么**：requirements.txt 列出项目全部依赖库及**精确版本号**；
.python-version 声明该项目用哪个 Python 版本。

**比喻**：菜谱只写"放盐"不行，得写"放 5 克盐"。
`pip install pandas` 是"放盐"，`pandas==3.0.3` 是"放 3.0.3 版的 pandas"。
版本不锁死，别人装到更新版本可能行为变了，复现不出你的结果。

**在本项目里干了什么**：
- 锁定了本项目实际验证过的 10 个库（pandas 3.0.3、streamlit 1.64.0、scikit-learn 1.9.0 等），
  别人一条 `pip install -r requirements.txt` 就装出一模一样的环境；
- 它还是云端部署的**硬要求**：Streamlit Cloud 按这个文件自动安装依赖，
  `.python-version` 里写的 3.12 告诉云端用哪个 Python。

### 32. streamlit-authenticator —— "门卫与登记处"

**它是什么**：Streamlit 生态里最常用的登录认证库，提供登录表单、注册表单、
密码哈希、图形验证码、Cookie 记住登录等功能。

**比喻**：给查询大厅配一位**门卫加登记处**。
门卫核对证件（用户名密码）、发一张**临时通行证**（登录 Cookie，
这样你刷新页面不用重新登录）；登记处负责给新访客建档（注册）。

**在本项目里干了什么**：
仪表盘的登录门禁，全部界面汉化（登录、注册、用户名、密码、验证码……）。
密码用 **bcrypt 哈希**存储——这是一种"单向加密"，只能验证不能还原，
即使数据库被看到也拿不到原始密码。

**踩到的两个真实的坑（都值得记住）**：

**坑一：`st.secrets` 是只读的，浅拷贝不够。**
预置账号放在 `st.secrets` 里，但库内部会往凭据字典**回写**哈希后的密码
（`credentials['usernames'][用户]['password'] = ...`），
于是报错 `TypeError: Secrets does not support item assignment`。
我原本用 `dict(...)` 做了浅拷贝，外层变成普通字典了，但**嵌套层仍是只读的**，
所以还是失败。最后写了个递归函数把整个结构深拷贝成普通字典才解决。

**坑二：库的注册功能不落盘，必须自己补。**
读源码发现 `register_user` 只把新用户加进**内存**里的字典，没有任何写文件动作
（我在源码里搜 `yaml`、`dump`、`open(` 全都没找到）。
而 Streamlit 有个特性：**每次交互都会重跑整个脚本、重建认证对象**——
意味着用户注册完点一下别的地方，账号就消失了。
所以我加了 `data/users.json` 做持久化：注册成功后把该用户的 bcrypt 哈希写进文件，
下次启动时与 secrets 里的预置账号合并。

**由此引出一个必须如实说明的限制**：
Streamlit Community Cloud 的**文件系统是临时的**，应用重启或重新部署后
`data/users.json` 会被重置，注册的账号会丢（secrets 里的预置账号不受影响）。
要真正长期保存用户，得接外部数据库（如 Supabase、PostgreSQL）——
这属于后续扩展，不是当前实现的能力，**不能吹成"完整的用户系统"**。

### 33. st.secrets 与 .gitignore —— "钥匙不能挂在门上"

**它是什么**：`st.secrets` 是 Streamlit 提供的密钥管理机制；
`.gitignore` 是告诉 Git "这些文件不要提交"的清单。

**比喻**：**钥匙不能挂在门上**。
你的仓库是公开的，把密码写进 `app.py` 就等于把家门钥匙插在门锁上，
还顺便在门口贴张纸告诉全网"钥匙在这"。
正确做法是把钥匙放在只有你能打开的地方（secrets），
并在 .gitignore 里写明"这个文件不许提交"，双保险。

**在本项目里干了什么**：
- 仓库里只提交 `.streamlit/secrets.toml.example`（**模板，不含任何真实凭据**）；
- 真实的 `.streamlit/secrets.toml` 与 `data/users.json` 都写进了 `.gitignore`，
  我用 `git check-ignore -v` 验证过确实被忽略；
- 线上部署时凭据在 Streamlit Cloud 的 **Settings → Secrets** 面板里填，
  不经过代码、不进仓库；
- 提交前还检查了 `users.json` 里**只有 bcrypt 哈希、没有明文密码**。

**一个真实的坑**：`.streamlit/secrets.toml` 是在项目早期提交过 `.gitignore` 之后才加的，
但要注意——**如果某个文件已经被提交过一次，之后再写进 .gitignore 是无效的**，
Git 会继续跟踪它。本项目里 `.zcode/` 目录就踩过这个坑（第一次提交时被收录了），
后来用 `git rm -r --cached .zcode` 把它移出跟踪才解决。

### 34. bcrypt —— "把密码搅成浆糊的搅拌机"

**它是什么**：一个专门为密码设计的哈希算法/库（Python 包就叫 `bcrypt`）。
streamlit-authenticator 内部的密码处理全靠它，本项目的注册与登录都经过它。

**比喻**：把密码**搅成浆糊**，且有两个关键特性：
1. **单向**：浆糊再也变不回鸡蛋——从哈希值**不可能**反推出原始密码；
2. **每次掺的"盐"都不同**：同一个密码每次哈希出来的结果都不一样
   （库里随机生成一段"盐"掺进去）。
   好处是攻击者没法用"预先算好的密码对照表"来反查——
   就像每把锁配独一无二的钥匙坯，抄来的钥匙图纸对不上别人家的锁。

bcrypt 还有第三招：**故意慢**（可调节的计算成本）。
验证一次密码要花约 0.1 秒，你登录时感觉不到，
但攻击者想每秒猜几百万次密码就变得不现实了。

**在本项目里干了什么**：
- 注册时：明文密码经 `Hasher.hash()` 变成 `$2b$` 开头的哈希串，存进 `data/users.json`；
- 登录时：把你输入的密码用存档里那把"盐"重新搅一遍，比对两份浆糊是否一致——
  全程不需要、也永远拿不到你的原始密码；
- 我提交前专门检查过：`users.json` 文件里**只有哈希、没有明文**。

**顺带澄清一个常见误区（面试可能被问）**：哈希**不是加密**。
加密是可逆的——拿钥匙能把密文解回原文；哈希是**不可逆**的——只能"再搅一遍对比"，
不存在"解密密码"这回事。凡是号称能"找回你的密码"的网站，都说明它存的是明文或可逆加密，
反而不安全。

### 35. captcha（图形验证码）—— "区分人和机器的小测验"

**它是什么**：生成验证码图片的小库（`captcha` 0.7.1），
streamlit-authenticator 的注册表单用它做人机校验。

**比喻**：门卫对访客出的**小测验**——一张扭曲变形的数字图片，
人眼一眼能认出来，简单脚本很难自动识别。

**在本项目里干了什么**：
注册表单的最后一步要求输入图片里的 4 位数字，目的是防止有人写脚本批量刷注册账号。
图片本身由 pillow 画出；库把正确答案**加密**存在服务器端的 session 里（不直接给浏览器）。

**顺带一个有意思的细节**：我写自动化测试时需要填这个验证码，
人是看图片，测试程序只能用 cookie_key 把加密答案解出来填入——
这也反过来证明了它的安全设计：答案不在页面上，脚本没法直接读。

**诚实说明它的局限**：它挡的是最粗糙的脚本机器人，
挡不住真人，也挡不住带图像识别的打码服务。
别在面试里把它说成"企业级防刷方案"——它是**低成本的常规防护**。

### 36. AppTest —— "汽车出厂前的检测线"

**它是什么**：Streamlit 官方自带的测试框架（`streamlit.testing.v1.AppTest`），
**不开浏览器**就能把整个应用从头到尾真实执行一遍，并检查每个环节。

**比喻**：**汽车出厂前的检测线**——车不用真开上路，
但油门、刹车、灯光逐项模拟检查一遍，有问题下线返修。

**在本项目里干了什么**（它是这次认证功能的质量保障）：
- 仪表盘上线前：模拟三个画像逐个切换，抓出了**"排行榜把流行度最低的排成第 1 名"**
  的排序 bug；
- 登录功能：验证了未登录时页面**零泄漏**（画像、歌手一个字都看不到）、
  正确密码能进、错误密码拦截、不存在用户拦截；
- 注册功能：验证了 `users.json` 正确落盘、密码是哈希不是明文、
  **全新会话**下注册用户能登录（证明持久化真的生效，不是活在内存里）；
- 还在测试中撞出了 `st.secrets` 只读那个坑。

一个如实说明的测试细节：AppTest 模拟不了真实浏览器的 Cookie，
所以测试日志里会出现 `Invalid token type` 提示——那是"没有历史登录凭证"的噪音，
真实服务器运行时没有，属已知无害现象。

### 37. PyInstaller —— "把整个厨房塞进一个饭盒"

**它是什么**：把 Python 程序连同解释器、所有依赖库打包成单个 exe 文件的工具。

**比喻**：**把整个厨房——灶台、锅碗、食材连厨师一起塞进一个饭盒**。
拿到饭盒的人自己不用有厨房（不用装 Python 和任何库），打开就能做饭。
代价是饭盒很大：本项目 exe 约 274MB，因为里面真的装了一整个 Python 科学计算环境。

**在本项目里干了什么**：
把网页服务器（Streamlit）、应用代码、登录认证和 22 张结果表全部封进
`性格歌手推荐器.exe`——双击 → 弹出控制台显示服务日志 → 自动打开浏览器 → 登录使用。
全程不需要安装 Python，也不需要联网。

**踩的三个坑（每个都记在这里，面试可讲）**：

1. **动态导入看不见**：Streamlit 的脚本执行器在运行时才导入
   `streamlit.runtime.scriptrunner.magic_funcs`，静态分析发现不了，
   打包后直接 `ModuleNotFoundError`。解决：在 spec 配置里显式声明 hiddenimports。
2. **数据文件不会自动打包**：认证库依赖的 Cookie 组件声明了自己的
   `frontend/build` 目录，验证码库要字体文件——代码打进去、数据没打进去，
   运行时报 `No such component directory`。解决：collect_data_files 逐个收集。
3. **打包后路径全变**：代码资源在只读临时目录（`sys._MEIPASS`），
   可写文件（注册用户、凭据）必须放在 exe 旁边。
   解决：路径解析按 `sys.frozen` 分流，launcher 启动时把工作目录切到 exe 所在处。

**如实说明的局限**：体积大；杀毒软件对 PyInstaller 程序误报是通病；
且 Streamlit 本质是网页应用——exe 双击后弹出的仍是浏览器页面，不是传统桌面窗口。

---

## 七、环境和存档

### 38. Anaconda —— "科学计算工具箱"

**它是什么**：一个打包好的 Python 发行版，预装了几百个科学计算库。

**比喻**：**买整套厨具套装**，而不是一口锅一把刀单独买。
装上就有 pandas、numpy、scikit-learn，省去一个个安装的麻烦。

**在本项目里干了什么**：
你的 Anaconda 装在 `E:\Anaconda`，PyCharm 里配置的就是这个环境。

> **一个真实情况**：实际运行时我检测到本机 Python 是 `D:\python\python`（3.12.6），
> 上面这些库也都在，所以项目直接用它跑通了。
> 如果 PyCharm 里配置的是 Anaconda 环境，结果完全一样，代码不用改。

---

### 39. Python 3.12 —— "干活的人"

**它是什么**：本项目的编程语言。

**比喻**：**会说各种工具语言的万能工人**。上面所有工具（pandas、sklearn 等）都是它的"技能包"。

**版本注意**：本项目用的是 Python 3.12 + pandas 3.0，比较新。
新版本有些行为变化（比如前面提到的字符串类型改名），
所以代码里做的兼容处理也算一个小亮点。

---

### 40. PyCharm —— "工作台"

**它是什么**：专业的 Python 集成开发环境（IDE）。

**比喻**：**木匠的工作台**。工具都摆在顺手的位置，还有自动补全、
错误提示、一键运行、断点调试等功能。

**在本项目里干了什么**：
你平时在 PyCharm 里看代码、改代码、运行脚本。
项目里的 `src/` 目录直接作为 PyCharm 项目打开就能用。

---

### 41. Git —— "游戏存档系统"

**它是什么**：版本控制工具，记录代码的每一次修改。

**比喻**：**单机游戏的存档功能**。
- 打 Boss 前存个档，打输了能读档重来
- 每次存档都有记录："第 3 关通关，2026-09-25 10:30"
- 想看看三天前怎么打的，随时能翻出来对比

**为什么必须用**：写代码经常改错东西，没有存档就只能靠"撤销"，
一关窗口就全没了。有了 Git，任何时候都能回到任何一个存档点。

**在本项目里干了什么**：
一共 **12 次存档（commit）**，按分析阶段划分：

```
① chore: 项目初始化（目录结构、zcode.md、.gitignore）
② data:  数据下载脚本与清洗EDA
③ analysis: 性格聚类
④ analysis: 画像分类
⑤ analysis: 流行度回归
⑥ analysis: 画像×歌手映射
⑦ docs:  README 与分析报告
⑧ fix:   修复图表缺陷
...
```

这样如果某一步改坏了，能精确回到上一步，不用全部重做。

**还解决了两个真实的坑**：
1. **E 盘权限问题**：E 盘的文件系统不记录文件所有权，Git 出于安全考虑报错
   `dubious ownership`（怀疑这仓库不是你的）。按官方建议把目录加入信任列表解决。
2. **数据文件不该进仓库**：原始数据有 7MB，而且是能重新下载的。
   我用 `.gitignore` 把它们排除，只在仓库里放了一个 `.gitkeep` 占位文件。

---

### 42. GitHub —— "云存档"

**它是什么**：存放 Git 仓库的网站。

**比喻**：**游戏的云存档**。
本地存档（Git）能防止改错代码，但如果**电脑硬盘坏了**，本地存档也没了。
云存档就是把存档同步到网上，换台电脑也能接着玩。

**这正是你提的需求"避免代码丢失"的完整答案**：
- 只有 Git（本地）→ 防手滑改错，不防硬盘损坏
- Git + GitHub → 两个都防

**在本项目里干了什么**：

仓库地址：**https://github.com/CJJ-008/pop-music-personality-analysis**

- 通过 GitHub API 自动创建了仓库（不需要你手动操作）
- 推送了 22 次提交、68 个文件（随项目推进持续更新）
- 设置了**公开**可见（简历项目需要给招聘方看）
- 验证了数据文件没有误上传

**顺便解决的一个网络问题**：
我测试发现你的 SSH 22 端口被网络拒绝了（国内常见），
而且你的 SSH 密钥是 **Gitee 的**（注释写着 `gitee-202408230140`），在 GitHub 上不能用。
所以最终走的是 **HTTPS + 凭证管理器**，凭证已保存，以后 `git push` 不用再登录。

---

### 43. Git Bash —— "命令行窗口"

**它是什么**：Windows 上的类 Linux 命令行环境。

**比喻**：**跟电脑用文字对话的窗口**。
图形界面是"点按钮"，命令行是"打字下命令"。
程序员更爱命令行，因为命令可以写成脚本自动执行。

**在本项目里干了什么**：
所有命令都是在这里执行的——下载数据、运行分析脚本、git 提交推送。

---

## 八、装了但没用上的工具（说清楚，免得你疑惑）

### PyTorch —— 深度学习框架

**它是什么**：做神经网络的框架。

**为什么没用**：你当初给了两个选项——
① 经典三件套（聚类+分类+回归）② 三件套 + PyTorch 神经网络加分项。
**你选了①**，理由是经典方法已经完整覆盖了简历需求。

**这个选择合理吗？合理。**
- 本项目的数据量是 1010 人问卷 + 28356 首歌，属于中小规模，
  神经网络的优势（处理海量非结构化数据）发挥不出来
- 面试官更看重"你懂不懂为什么用这个方法"，而不是"你会不会调神经网络"
- 真要加，可以后续做一个小 MLP 对比实验，成本不高

### Java —— 编程语言

**为什么没用**：数据分析生态在 Python 上最成熟（pandas、sklearn 都是 Python 的）。
Java 更适合做后端服务和大数据工程（Hadoop、Spark 生态）。
本项目是**分析型任务**，用 Python 是标准选择。

---

## 九、一张表看完所有工具

| 工具 | 通俗定位 | 在本项目干的活 |
|---|---|---|
| **Kaggle** | 数据集超市 | 提供 1010 人性格问卷 |
| **TidyTuesday** | 每周食材包 | 提供 32833 首歌数据 |
| **kagglehub** | 外卖取件员 | 自动下载问卷（免注册） |
| **urllib** | 自己开车取货 | 下载 Spotify CSV |
| **pandas** | Excel 超级加强版 | 清洗、填补、合成、分组、合并、去重 |
| **NumPy** | 计算引擎 | 数组运算、固定随机种子 |
| **scikit-learn** | 侦探事务所 | 8 种算法：聚类/分类/回归 |
| **K-Means** | 聚会自动分堆 | 分出 3 个性格画像 |
| **层次聚类** | 画家谱 | 交叉验证分组结果（ARI=0.364） |
| **PCA** | 给立体雕像拍照 | 压成 2D 画聚类散点图 |
| **逻辑回归** | 画分界线 | 分类，准确率 67.6% |
| **决策树** | 医院分诊台 | 分类，准确率 57.3% |
| **随机森林** | 400 个专家投票 | 分类 69.2%（最佳）、回归 R²=0.173 |
| **线性回归** | 画趋势线 | 回归基线，R²=0.087 |
| **StandardScaler** | 统一度量衡 | 聚类和回归前标准化 |
| **交叉验证** | 模拟考试 | 5 折验证，防过拟合 |
| **特征重要性** | 复盘排线索 | 找出关键特征（极限运动、发行年份） |
| **SciPy** | 质检员 | 提供统计检验 |
| **Kruskal-Wallis** | 判断差异真假 | 17 个流派中 15 个差异显著 |
| **Matplotlib** | 基础画笔 | 画全部 18 张图 |
| **Seaborn** | 精装画笔套装 | 热力图、箱线图美化 |
| **Jupyter** | 实验记录本 | 26 单元格分析报告 |
| **nbformat + nbconvert** | 自动排版与分发 | 用代码生成报告；导出离线 HTML 挂进仪表盘 |
| **CSV/utf-8-sig** | 通用表格 | 22 张结果表，Excel 不乱码 |
| **Anaconda** | 厨具套装 | Python 环境 |
| **Python 3.12** | 干活的人 | 编程语言 |
| **PyCharm** | 工作台 | 写代码、运行 |
| **Git** | 游戏存档 | 22 次提交，分阶段存档 |
| **GitHub** | 云存档 | 异地备份，防硬盘损坏 |
| **Git Bash** | 命令行窗口 | 执行所有命令 |
| **Streamlit** | 自助点播查询机 | 歌手推荐器网页 app.py；AppTest 冒烟测试三画像 0 异常 |
| **Plotly** | 会动的图表 | 仪表盘里的性格雷达图、歌手流行度条形图 |
| **requirements.txt** | 配方的精确刻度 | 锁定 10 个库的版本，别人一条命令装出相同环境，云端部署必需 |
| **streamlit-authenticator** | 门卫与登记处 | 登录/注册界面、bcrypt 密码哈希、图形验证码、Cookie 记住登录 |
| **st.secrets + .gitignore** | 钥匙不能挂在门上 | 凭据不进公开仓库；只提交 .example 模板，真实 secrets 与 users.json 均被忽略 |
| **标签 × 流派亲和度推荐** | 按性格拼图点菜 | 自研四步推荐链路：37标签→参考人群→流派亲和度→歌手；预计算矩阵存结果表 |
| **KNN 最近邻测评推荐** | 相亲节目的性格匹配 | 25题自测→五维z标准化→欧氏距离取最近101人→用其真实偏好推荐 |
| **PyInstaller** | 把整个厨房塞进一个饭盒 | 打包 274MB 单文件 exe，双击即用；解决动态导入/数据文件/路径三大坑 |
| **bcrypt** | 搅碎密码的搅拌机 | 单向 + 加盐 + 故意慢；users.json 里只有 $2b$ 开头的哈希、无明文 |
| **captcha（图形验证码）** | 人机小测验 | 注册表单的 4 位数字验证码，挡简单脚本批量刷号 |
| **AppTest** | 出厂检测线 | 不开浏览器真实执行应用；登录/注册全流程测试，抓出排序与只读两个 bug |

---

## 十、面试速查：三句话讲清一个工具

如果面试官问"你用了什么技术"，不要背工具名，讲**为什么用**：

- **pandas**："问卷 150 列有 144 列缺失，我用中位数和众数分别填补，
  还把 7 道相关题目平均成 1 个性格维度——这是数据清洗和特征工程的部分。"
- **scikit-learn**："聚类用 K-Means，但轮廓系数只有 0.17，
  说明人群性格本来就没有清晰分界，我如实写进报告了，
  没有硬说成'发现了 3 种人格类型'。"
- **Kruskal-Wallis**："我没有只看均值差异就下结论，
  用非参数检验筛了一遍，17 个流派里 15 个显著、2 个不显著，
  不显著的就没当成结论。"
- **去重**："回归前我按 track_id 去重了，因为原始数据 4477 行是同一首歌重复出现，
  不去重会让同一首歌同时进训练集和测试集，R² 虚高——这是信息泄漏。"
- **Git**："按分析阶段分了 22 次提交，原始数据用 .gitignore 排除，
  仓库 68 个文件，克隆后跑一个下载脚本就能完整复现。"
- **Streamlit**："仪表盘只读已入库的结果表、不碰原始数据，所以克隆仓库或部署上云后
  不用重跑分析，冷启动就能用；上线前用官方 AppTest 框架跑了三个画像的冒烟测试，
  还真抓出一个'排行榜把低分排第一'的排序 bug。"
- **登录认证**："凭据放 st.secrets、只提交 .example 模板，密码用 bcrypt 哈希存储，
  提交前验证过 users.json 里没有明文。注册功能有个坑：库只更新内存字典不落盘，
  而 Streamlit 每次交互都会重建对象，所以我读了源码确认后自己加了 JSON 持久化，
  否则用户注册完一刷新就没了。"
- **诚实说明限制**："Streamlit Cloud 的文件系统是临时的，重启后注册账号会丢，
  要长期保存得接外部数据库——这是我知道但当前没做的事，不会说成'完整的用户系统'。"

**最值得强调的一句话**：
这个项目里我花最多精力的不是调模型，而是**避免得出错误结论**——
去重防泄漏、检验防噪声、相对偏好防假象、如实披露低轮廓系数。
